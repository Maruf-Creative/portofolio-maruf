
  # Duplicate design request

  This is a code bundle for Duplicate design request. The original project is available at https://www.figma.com/design/draH582LVAqDiEMhdmsSvo/Duplicate-design-request.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  